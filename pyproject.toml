[project]
name = "job-tracking"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "httpx",
    "pyyaml",
    "fastapi",
    "uvicorn",
    "libsql",
]

[tool.vercel]
entrypoint = "board.app:app"
